# Production environment variables for frontend
# API calls will use relative paths in production (Vercel)
VITE_API_BASE_URL=/api

# Disable story continuation feature (has issues with serverless cold starts)
VITE_ENABLE_STORY_CONTINUATION=false
