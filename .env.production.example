# Production Environment Variables Template
# Copy this file to configure your Vercel deployment
# Set these variables in Vercel Dashboard: Settings > Environment Variables

# ============================================================================
# REQUIRED VARIABLES
# ============================================================================

# OpenRouter API Configuration
# Get your API key from: https://openrouter.ai/keys
OPENROUTER_API_KEY=your_openrouter_api_key_here

# ElevenLabs API Configuration
# Get your API key from: https://elevenlabs.io/
ELEVENLABS_API_KEY=your_elevenlabs_api_key_here

# ============================================================================
# OPTIONAL VARIABLES (with defaults)
# ============================================================================

# Application Environment
# Options: production, preview, development
ENVIRONMENT=production

# OpenRouter Model Configuration
# Default model for horror story generation
OPENROUTER_MODEL=gpt-3.5-turbo

# Logging Configuration
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# ============================================================================
# NARRATION SETTINGS (optimized for Vercel free tier)
# ============================================================================

# Enable/disable narration feature
# Set to false to disable narration and reduce API costs
NARRATION_ENABLED=true

# Maximum concurrent narration requests
# Keep at 1 for free tier to avoid timeout issues
NARRATION_MAX_CONCURRENT=1

# Maximum content length for narration (characters)
# Shorter content = faster generation = less timeout risk
NARRATION_MAX_CONTENT_LENGTH=5000

# Narration timeout (seconds)
# Must be less than Vercel's 10-second function timeout
NARRATION_TIMEOUT=9

# ============================================================================
# PERFORMANCE SETTINGS
# ============================================================================

# Enable response caching
# Note: Vercel KV not available on free tier
# This enables in-memory caching within function execution
ENABLE_CACHING=false

# External storage URL (optional)
# For narration audio caching using free services like Cloudinary
# Leave empty to disable external storage
EXTERNAL_STORAGE_URL=

# ============================================================================
# SECURITY SETTINGS
# ============================================================================

# CORS allowed origins (comma-separated)
# Update with your production domain
CORS_ORIGINS=https://your-domain.vercel.app

# Rate limiting (requests per minute per IP)
RATE_LIMIT_PER_MINUTE=100

# Enable debug mode (should be false in production)
DEBUG=false

# ============================================================================
# DEPLOYMENT NOTES
# ============================================================================
# 
# To set these in Vercel:
# 1. Go to your project in Vercel Dashboard
# 2. Navigate to Settings > Environment Variables
# 3. Add each variable with appropriate scope (Production/Preview/Development)
# 4. Redeploy your application for changes to take effect
#
# For sensitive variables (API keys), use Vercel's encrypted storage
# Never commit actual API keys to version control
#
